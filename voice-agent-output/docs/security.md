# Security Documentation: enterprise-grade-real-time

## Security Posture Summary

| Control | Setting |
|---------|---------|
| Authentication | managed_identity |
| Compliance Framework | hipaa_guidance |
| Network Model | private |
| Data Classification | confidential |
| Encryption at Rest | Enabled |
| Encryption in Transit | Enabled |
| Secret Management | Key Vault |

## STRIDE Threat Model

| ID | Category | Threat | Mitigation | Residual Risk |
|----|----------|--------|------------|---------------|
| THREAT-001 | Spoofing | Unauthorized entity impersonates a legitimate service or user to access resources. | Managed Identity for service auth. Entra ID for user auth. No shared secrets. | Low |
| THREAT-002 | Tampering | Malicious modification of data in transit or at rest. | TLS 1.2+ enforced. Encryption at rest via Azure platform encryption. Immutable audit logs. | Low |
| THREAT-003 | Information Disclosure | Sensitive data exposed through logs, error messages, or misconfigured access. | Structured logging without PII. Key Vault for secrets. Private networking. RBAC enforcement. | Low |
| THREAT-004 | Denial of Service | Resource exhaustion through excessive requests or payload abuse. | Container Apps auto-scaling with max replica limits. Request size limits. Rate limiting at API layer. | Medium |
| THREAT-005 | Elevation of Privilege | Attacker gains higher privileges than authorized through misconfigured RBAC or container escape. | Least-privilege RBAC. Non-root containers. No privileged capabilities. Regular access reviews. | Low |
| THREAT-006 | Tampering | Prompt injection attacks to manipulate AI behavior or extract training data. | Content safety filters. Input validation. Output sanitization. System prompt hardening. | Medium |


## Security Controls by Component


### container-app (Azure Container Apps)
- Managed Identity
- HTTPS Only
- Private Ingress
- Min TLS 1.2

### key-vault (Azure Key Vault)
- RBAC Access
- Soft Delete
- Purge Protection
- Diagnostic Logging

### log-analytics (Azure Log Analytics)
- Data Retention Policy
- Access Control
- Diagnostic Settings

### managed-identity (Azure Managed Identity)
- Least Privilege RBAC
- No Credential Storage

### container-registry (Azure Container Registry)
- Managed Identity Pull
- Private Access
- Image Scanning

### cosmos-db (Azure Cosmos DB)
- Managed Identity
- Encryption
- Private Endpoint

### redis-cache (Azure Redis Cache)
- Managed Identity
- TLS Only
- Private Endpoint
- Non-Public Access


## RBAC Role Assignments

| Principal | Resource | Role | Justification |
|-----------|----------|------|---------------|
| Managed Identity | Key Vault | Key Vault Secrets User | Read application secrets |
| Managed Identity | Storage Account | Storage Blob Data Contributor | Read/write blob data |
| Managed Identity | Container Registry | AcrPull | Pull container images |
| GitHub Actions (OIDC) | Subscription | Contributor | Deploy infrastructure |

## Least Privilege Guidance

1. **Never use Owner role** for service principals or managed identities
2. **Scope roles to resource groups**, not subscriptions, where possible
3. **Use custom roles** if built-in roles are too broad
4. **Review access quarterly** and remove unused assignments
5. **Enable PIM** for just-in-time privileged access

## Supply Chain Security

- **CodeQL**: Automated code scanning on every PR and weekly schedule
- **Dependabot**: Automated dependency updates for pip, GitHub Actions, and Docker
- **Non-root containers**: Application runs as unprivileged user
- **Multi-stage Docker builds**: Minimal attack surface in production image

---
*Generated by Enterprise DevEx Orchestrator Agent*
